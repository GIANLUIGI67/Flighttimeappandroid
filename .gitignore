*.iml
.gradle/
local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild/
/app/build

# Firebase config (contains API keys — embedded in app bundle at build time, not needed in repo)
google-services.json
app/google-services.json

# Release artifacts
app/release/
*.aab
*.apk
